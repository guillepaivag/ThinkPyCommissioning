These files are engineering reference material only.

They are NOT read by the runtime agent.
They are NOT indexed.
They are NOT used for RAG.
They are NOT a source of runtime validation decisions.

Engineering decisions derived from these documents must be
reviewed and encoded into the approved protocol YAML before runtime.
